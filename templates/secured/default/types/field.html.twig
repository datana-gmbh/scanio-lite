{# @var field \App\Crud\Domain\Value\Field #}

{% set value = get_value(item, field.propertyPath) %}

{% if value is not null %}
    {% if field.type.value == 'id' %}
        {% include 'secured/default/types/id.html.twig' %}
    {% elseif field.type.value == 'boolean' %}
        {% include 'secured/default/types/boolean.html.twig' %}
    {% elseif field.type.value == 'text' %}
        {% include 'secured/default/types/text.html.twig' %}
    {% elseif field.type.value == 'url' %}
        {% include 'secured/default/types/url.html.twig' %}
    {% elseif field.type.value == 'date' %}
        {% include 'secured/default/types/date.html.twig' %}
    {% elseif field.type.value == 'datetime' %}
        {% include 'secured/default/types/datetime.html.twig' %}
    {% elseif field.type.value == 'integer' %}
        {% include 'secured/default/types/integer.html.twig' %}
    {% else %}
        {{ item[field.propertyPath] }}
    {% endif %}
{% else %}
    -
{% endif %}
